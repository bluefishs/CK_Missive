# 全系統覆盤 — 2026-08-01（各模組 × 議題 × 架構）

> **範圍**：透過已建立的六階自我檢核機制，對全系統各模組做一次完整複查。
> **方法**：所有數字為當次實跑輸出；未量測或工具失效者明確標示。
> **配套標準**：`SELF_AUDIT_EVOLUTION_STANDARD.md`（本次同步立法）

---

## 1. 系統基線（實測）

| 面向 | 數值 | 判定 |
|---|---|---|
| 容器 | 55 Up / **0 非健康** | ✅ |
| 五系統公網 | missive・lvrland・pilemgmt・digitaltwin・www **全 200** | ✅ |
| 業務量 | documents 1970 / KG 49,508 | ✅ |
| producer 契約 | **18 GREEN / 0 blind spot** | ✅ |
| UI 流程（深度 7 條） | **7/7 PASS** | ✅ |
| UI 全站（廣度 87 條） | **86 PASS / 0 FAIL / 1 已知環境限制** | ✅ |
| 導覽列 live | 死連結 0 / 重複路徑 0 | ✅ |
| case_code 橋接 | **87/87 = 100%** | ✅ |
| fitness | 78 步 | — |

---

## 2. 各模組現況

### 2.1 標案 → 建案 → 成案 → 財務 → 核銷（本輪主戰場）

五個斷點全部修復並驗證：

| 斷點 | 修法 | 驗證 |
|---|---|---|
| ezbid 入口（37,980 筆進不了鏈路） | `job_number` 改選填、`ezbid:{id}` 識別、補建案鈕 | UI 檢核 PASS |
| 建案防重複失效 | 候選偵測 + 三道查重 + 關聯既有 | 實測 409 擋下 |
| 標案↔案件無回指 | `source_tender_id`（migration `20260731a001`） | 187 已關聯 8526 |
| 財務接續斷 | 報價可帶入來源標案預算 | `by-id` 回 22,675,000 |
| 呈現不一致 | `TenderActionBar` 兩分支共用 | regression 鎖住 |

**架構結論**：號碼體系是流程產物，但案件常「從中途進來」。已用**方案 B**
（成案即創財務號）從源頭止血 + **自我修復 job** 補既有缺口 → 覆蓋率 100%。
**方案 C**（收斂為單一識別碼）維持不建議，成本 > 收益。

### 2.2 核銷掃描管線

| 問題 | 真因 | 狀態 |
|---|---|---|
| 收據影像看不到 | `service.create()` 無 `receipt_image_path` 參數，端點卻用 kwarg → TypeError → auto_create 全 500 | ✅ 修（路徑改放 schema） |
| 金額 940 記成 957 | **QR 資料本身與紙本不符**（差額＝加油金折抵，商家 POS 折抵前產生 QR） | ✅ 偵測即警示、**不自動改** |
| 手機批次被中斷 | 建立成功一律 navigate 清單 | ✅ 「建立並繼續掃下一張」 |
| 行動版重點資訊 | 窄螢幕捲動易漏看金額 | ✅ 大字摘要卡 + 金額異常橘字 |

### 2.3 通知 / 告警

| 指標 | 之前 | 之後 |
|---|---|---|
| 每日新增告警 | +66（近 7 日恆定） | **0 新增**（連兩次掃描 111→111） |
| 未讀通知 | **4708** | 731 |
| 過去仍 pending 事件 | 690 | **0** |

**架構結論**：問題不是「告警太少」而是「告警沒有價值」。修法三刀＝
去重（穩定 key，不可用逐日變動的 title）／逾期分級（90 天）／歷史歸檔（可逆）。

### 2.4 坤哥意識體服務鏈

| 層 | 實測 |
|---|---|
| 資料 | KG 49,508 實體 / 48,942 有 embedding（**98.9%**） |
| 活動 | 近 7 日 agent 查詢 199、工具呼叫 136；累計學習 1,976、進化史 118 |
| 記憶 | diary 101 / patterns 12 / proposals 9 / crystals 4 / 自省 120（皆今日更新） |
| 端點 | patterns・proposals・crystals・diary・autobiography・evolution-status **6/6 200** |
| 頁面 | 五主軸（對話/心智/進化/圖譜/運維）**全在**，進化分頁可載入 |

> `kunge/snapshot` 與 `memory/digest` 回 401 屬**正確**（需 `X-Service-Token`，供 Hermes 聯邦）。

### 2.5 site-management / 導覽

新增 live 完整性稽核（fitness step 78）：死連結 0、重複路徑 0、
13 條無導覽入口的頁面（多為刻意，如 `/kunge/*` 子分頁）。

> 初版誤報「/documents 被兩項目共用」，開 DB 核實為**父子關係**（合理）→ 已加豁免。
> 若照報告直接刪項目，會弄壞正常導覽結構。

### 2.6 測試與 CI

| 項目 | 現況 |
|---|---|
| 後端全測試 | **52 個既有失敗**（已用 stash 對照確認與本輪無關） |
| 前端關鍵測試 | ERPExpensePages 27 紅 → **24 綠**；新增 4 支 regression 全綠 |
| 測試安全網 | conftest 抽憑證式（**三版才對**），含 runtime 驗證 |

---

## 3. 仍未解決的議題（依優先序）

| # | 議題 | 數據 | 建議 |
|---|---|---|---|
| **A1** | **價值層無自動機制** | 告警噪音靠 owner 貼訊息才發現 | 見標準 §5；把「產出被使用程度」變 metric |
| **A2** | 後端 52 個既有測試失敗 | 涵蓋 case_code/pm_case/wiki/multichannel 等 | 需獨立 session；目前無保護力 |
| **A3** | `dead_ui_detector` 143 候選 | 閾值 10、後端 326 端點 / 前端 1418 字面 | 偵測器噪音大，**需先驗鑑別力**再採信（標準 §3） |
| **A4** | service entropy 21.1% | 閾值 20%，wiki_* 等散戶 | 低風險技術債 |
| **A5** | ADR active 20 | 目標 ≤15；ADR-0020 仍 PROPOSAL 逾期 2 月 | 一次性 triage |
| **A6** | H4 同名元件 4 組 | MermaidBlock（props 不同）/ CategoryPieChart（**3 份**）/ ExpensesTab / StaffTab | 收斂前需驗行為等價 |
| **A7** | `capability_usage_audit` 輸出為空 | 工具本身失效 | 屬 A1 的關鍵輸入，應先修 |
| **A8** | `/admin/deployment` 503 | 未配置 GITHUB_TOKEN | owner 決定補 token 或維持 |

---

## 4. 本輪建立的機制（可重用資產）

| 機制 | 檔案 | 用途 |
|---|---|---|
| UI 流程檢核（深度） | `ui_flow_smoke.cjs` | 7 條曾回報過的流程 |
| UI 全站掃描（廣度） | `ui_page_sweep.cjs` | 87 條靜態路由 |
| 檢核用臨時 session | `ui_smoke_auth.py` | 自簽 20 分鐘，不碰他人登入 |
| 一鍵入口 | `run_ui_smoke.sh` | `--sweep` 切換廣度 |
| 每日排程 | `install-ui-smoke-task.ps1` + `run-ui-smoke.cmd` | 04:15 / 04:30 |
| 檢核新鮮度 | `ui_smoke_freshness.py`（step 77） | 檢核器自己停跑也會被抓 |
| 導覽 live 稽核 | `navigation_live_integrity_audit.py`（step 78） | 死連結 / 重複路徑 |
| 同名元件偵測 | `heterogeneous_work_audit.py` H4 | 零誤判設計 |
| 自我修復 | `case_finance_bridge_selfheal`（每日 02:50） | 檢核 → 修復 |

---

## 5. 自我檢視

1. **本次覆盤過程中，3 支既有審計在 host 上根本跑不起來**
   （`service_dir_entropy` / `dead_ui_detector` cp950 編碼、`capability_usage_audit`
   容器名寫死）—— 也就是說它們**已經失效一段時間而沒人知道**。已修前兩支。
   這本身就是 A1（價值層）的證據：機制存在 ≠ 機制有用。
2. **A3 的 143 個 dead UI 候選我沒有逐一核實**，僅依標準 §3 標記為「需先驗鑑別力」。
   若其中有真的死端點，本次沒有處理。
3. **A2 的 52 個測試失敗我只做了分類、沒有修**，本輪所有「測試綠」的宣稱
   都只涵蓋針對性測試，不是全量。
4. 六階階梯的第 6 階（價值層）**我提出了問題但沒有給出可執行的機制**，
   只說了「不能靠再加告警」。這是本文最弱的一段。
5. 標準文件 §3 的 7 次假訊號**全部發生在兩天內、且多數是我自己造成的**。
   把它寫進標準是對的，但這也代表我在這兩天的產出品質不穩定。
