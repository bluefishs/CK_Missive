# ERP 金流鏈複查與近期議題統整（2026-09-04）

> owner 09-04：「請統整近期議題與紀錄，部分尚未完善且須複查前後端服務與架構涉及對應程序」。
> 本檔是**入口**：每條鏈寫「寫入點 → 讀取點 → 自動化橋 → 守門 → 今天發現的斷點」。
> 逐項細節在 `OPEN_ITEMS_20260819.md`（第十二～十四輪、A90–A97）；欄位語意在 `FIELD_SEMANTICS.md`。

## 0. 這一天反覆出現的形狀

**每張表單獨看都正常，鏈才是斷的。** 六個回報，沒有一個是某個端點報錯：

| 回報 | 寫在哪 | 讀在哪 | 中間缺什麼 |
|---|---|---|---|
| 協力廠商加了 500,000，廠商帳款看不到 | `project_vendor_association` | `erp_vendor_payables` | 沒有橋 ⇒ **指派即應付** |
| 委託單位 186 家，頁面只有 50 家 | 後端 `limit` 預設 50 | 頁面在那 50 筆上分頁 | 沒人送 `limit`，也沒人警告截斷 |
| 167 有發票卻「無按鈕也無顯示」 | `erp_invoices.billing_id` | 分頁「發票號碼」欄 `hideOnMobile` | 兩欄互斥，窄螢幕都空 |
| 168「發票已填報」 | —（**資料庫沒有這張發票**） | 分頁 | 「已收款」可以不帶發票、不帶日期 |
| 竹崎地政無法點 | 承攬案文字客戶 | 帳款第二腿 `vendor_id=None` | 名稱沒對主檔 |
| 統編一欄兩義 | 協力廠商填 `vendor_code`、委託單位填 `tax_id` | 兩頁各讀各的 | 沒有宣告哪一欄是統編 |

## 1. 應收鏈：成案 → 請款 → 發票 → 收款 → 帳本

| 環節 | 寫入點（後端） | 前端入口 | 自動化橋 | 守門 |
|---|---|---|---|---|
| 成案 | `contract/case_code.promote_to_project` | PM 案「轉成案」／回簽上傳後詢問 | 成案即應收：`ERPBillingService.ensure_first_period`（一次請領＝總價） | weekly 103、105 |
| 請款 | `ERPBillingService.create/update`（paid 必帶金額＋日期，**只在碰到付款欄位時擋**） | `/erp/quotations/:id/accounts/receivable/*` | 收款 ⇒ EventBus `billing_paid` ⇒ `finance_ledgers` | weekly 104 ①③ |
| 發票 | `ERPInvoiceService.create_from_billing`（**09-04 起**：格式 2 英文＋8 數字、全庫重複擋在服務層） | 應收分頁「發票號碼」欄的「開立發票」 | 一筆請款一張票；`invoice_number`／`invoice_date`／`invoice_amount` 回到請款回應 | weekly 104 ②⑤⑦⑧、**⑪ 已收款無發票（新）** |
| 收款 | 同請款 update | 同上 | 帳本 AR | weekly 97（帳本 case_code 可達） |

**今天的斷點與修法**：
- 167：前端兩欄互斥 ⇒ 合成一欄「發票號碼」（有票印號碼＋tooltip，沒票給按鈕）。
- 168：第一期 7,936,250 標 `paid`、`payment_date` 空、無發票。系統此前**接受**這個狀態且畫面沒有任何提示。
  現在：列上「已收未開票」標籤＋分頁頂端警示＋weekly 104 ⑪。**不自動補**——發票號碼只有人知道（A97）。
- 存量：已收款無發票 6 筆 2,114 萬（152／161／168…）。

**仍未完善（要 owner 決定）**：
- 收款時要不要**硬擋**「沒有發票不得標已收」？政府機關案的慣例是發票隨請款送出，硬擋合理；
  但存量 6 筆會先卡住編輯。現在只警示。
- `create_from_billing` 的稅額固定 0；實測 155/170 張發票有稅額、15 張為 0。要不要預設 5%（含稅反推）？

## 2. 應付鏈：指派 → 應付 → 付款 → 帳本

| 環節 | 寫入點 | 前端入口 | 自動化橋 | 守門 |
|---|---|---|---|---|
| 指派 | `project_vendors.py`（建／改／刪） | 承攬案「協力廠商」分頁 | **指派即應付**（09-04 新）：`ERPVendorPayableService.ensure_from_association` | **weekly 106（新）** |
| 應付 | `ERPVendorPayableService.create/update`（防重＝同報價＋同廠商＋同金額＋同期別） | 報價單 `?tab=payable`、`/erp/vendor-accounts` | 付款 ⇒ 帳本 AP | weekly 78（上限）、99（`billing_id`）、104 |
| 付款 | 同上 | 同上 | `_sync_ledger_if_paid` | weekly 97 |

**今天的斷點與修法**：兩表沒有橋（16 案有指派 13 案無應付）。自動建的帶 `[auto:vendor_association]`，
人工建的不碰；指派金額與人工應付不一致 3 筆＝A96。⚠️ 首版排除 `quote_kind=tender` 讓 01 委辦案四筆全被略過——
01 案的應付本來就掛在投標報價單上。

## 3. 報價鏈：建單 → 工項 → 輸出 → 回簽 → 成案

| 環節 | 寫入點 | 前端入口 | 自動化 | 守門 |
|---|---|---|---|---|
| 建單 | `quotations.py create` | 案件「報價單」分頁（建立頁只建單） | 有報價單就不再給「新增報價」 | 105 |
| 工項 | `quotation_items` replace | 分頁明細（項次可自填、複價可覆寫、備註） | 改工項 ⇒ 已輸出檔自動重產 | 104 ⑨ |
| 輸出 | `QuotationDocumentService.render_xlsx/pdf/archive` | 列印／輸出（單一入口） | 存入附件 `doc_type=generated_quotation` | 探針 |
| 回簽 | `pm/attachments upload doc_type` | 分頁附件區常駐上傳（01 契約文件／02 客戶回簽） | 上傳後詢問是否已承攬 ⇒ PM 轉 contracted ⇒ 成案 ⇒ 成案即應收 | 103、105 |

## 4. 主檔與識別

- **委託單位主檔**（`partner_vendors.vendor_type=client`）196 家；承攬案 `client_agency` 不在主檔＝0（09-04 補 7 家）。
- **統一編號**＝`tax_id`；`vendor_code`＝內部代碼。協力廠商 15 家已搬；查廠商兩欄都認、`tax_id` 優先。
- 帳款彙總的第二腿（只有名字的客戶）09-04 起以名稱對主檔補 `vendor_id`，所以可點進明細。

## 5. 列表頁三要素現況（§2.6）

| 頁 | 統計卡 | 互動篩選 | 當年度 | 09-04 修的 |
|---|---|---|---|---|
| `/erp/quotations` | ✓（後端全量） | 年度／類別／委託單位／關鍵字 | ✓ | 固定只列成案；案件狀態→委託單位；工具列去重 |
| `/erp/client-accounts` | ✓ | 年度／關鍵字／卡片 | ✓ | **只取 50 家 ⇒ 取全＋截斷警示** |
| `/erp/vendor-accounts` | ✓ | 年度／關鍵字／卡片 | ✓ | 統編欄改 `tax_id` |
| `/contract-cases` | ✓（跟篩選） | ✓ | ✓ | 統計卡接篩選（第五輪） |
| `/pm/cases` | ✓ | ✓ | ✓ | 報價總額卡跟狀態卡（第四輪） |

> 09-04 晚：owner 裁示「表格可篩選排序，就不要重複做獨立篩選欄位」⇒ §2.6 ④；`/contract-cases` 已改為參考實作，其餘頁＝A98。

## 6. 待 owner 的清單（09-04 新增）

| 項 | 內容 | 在哪 |
|---|---|---|
| A90 | 同名案 9 筆（8 筆是 a／b／c 子案，已成案；006 已刪） | ✅ 結案 |
| A92 | 229 張總價更正 | ✅ 已執行 |
| A96 | 指派金額 vs 人工應付不一致 3 筆 | weekly 106 YELLOW |
| A97 | 已收款無發票 6 筆 2,114 萬（168 第一期還缺收款日期） | weekly 104 ⑪ YELLOW |
| — | 收款是否硬擋「無發票」；發票稅額預設 | 本檔 §1 |

## 7. 今天沒做、刻意不做的

- 帳款兩頁**沒有**改成伺服器端分頁排序：每家一列的彙總最多幾百筆，取全＋截斷警示比 server-side sort 少一層可錯的東西；
  資料真的長到千筆再做（截斷警示會先出聲）。
- 沒有自動補 168 的發票或收款日期：兩者都只有人知道。
