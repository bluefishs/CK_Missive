# 專案資金鏈：報價 → 專案 → 財務

> 建立：2026-08-27
> owner：「針對報價、專案管理與財務整體架構請逐一完善，以利掌握公司專案資金管理」
>
> **所有數字都是 2026-08-27 實測。**

---

## 1. 鏈路實況

```
邀標案件 pm_cases 253（已成案 50）
   └─ case_code ─┐
                 ├→ 報價單 erp_quotations 256（已確認 251）
                 │     └─ erp_quotation_id ─┬→ 請款 erp_billings 59（已收款 38）
                 │                          └→ 應付 erp_vendor_payables 37（已付 33）
                 │                                    │
   承攬專案 contract_projects 88 ────────────────────┤
   （project_code + case_code，已結案 75／執行中 13）  │
                                                      ↓
                              統一帳本 finance_ledgers 81（收入 39／支出 42）
                              來源：erp_billing 39｜erp_vendor_payable 33｜expense_invoice 9
```

### ⚠️ 關聯鍵的實情：請款與應付掛在**報價單**，不是專案

`erp_billings` 與 `erp_vendor_payables` **只有 `erp_quotation_id`**，
沒有 `project_code`。要看某個專案的資金必須走**三跳**：

```
contract_projects.project_code → .case_code → erp_quotations.case_code
                                            → .id → billings / payables
```

⇒ 任何「這個專案收了多少付了多少」的查詢都要記得這一跳，
**漏掉就會得到 0 而不是報錯**。

---

## 2. 帳本層：健康

| 檢查 | 結果 |
|---|---|
| 已收款請款 38 筆 → 沒進帳本 | **0** |
| 已付應付 33 筆 → 沒進帳本 | **0** |
| 核銷 9 筆 → 沒進帳本 | **0** |
| 帳本有而來源不存在（孤兒） | **0** |

⚠️ 我第一次查得到「38/33 沒進帳本」——**那是假象**，因為我把
`source_type` 猜成 `billing`／`payable`，實際是 `erp_billing`／
`erp_vendor_payable`。**用猜的值查詢會得到一個看起來很嚴重的假結論。**

---

## 3. 真正的缺口在上游：健康的帳本回答不了「該進來的有沒有進來」

88 個承攬專案裡 **37 個（42%）完全沒有任何請款或應付紀錄**。
拆開之後是**兩群完全不同的東西**：

| 群 | 案數 | 合約金額合計 | 判讀 |
|---|---:|---:|---|
| 已結案且無金流 | 28 | **$0** | 合約金額全部未填，2025-03~05 舊資料 ⇒ 歷史缺口 |
| **執行中且無金流** | **9** | **$11,555,000** | **錢該收而系統裡沒有紀錄** |

其中有明確合約金額的 4 案：

```
CK2023_01_01_001  $5,630,000  112至113年度桃園市轄內興辦公共設施工程
CK2024_01_01_002  $4,000,000  112至113年度桃園市轄內興辦公共設施工程
CK2026_01_01_005  $1,300,000  115年度南投縣都市計畫樁位測補釘案委託技術
CK2026_01_01_011    $625,000  和美鎮84年TWD67重測區圖根點補建
```

另 5 案合約金額為 0 或未填（可能還沒簽約），**刻意不算進告警**——
把它們算進來會讓這條每天報一個不能行動的數字。

### 已納管

`business_vital_signs` 第 12 條「執行中且有合約金額的案子，有在走金流」，
判 **YELLOW**（業務推進不是系統故障），現況 **4（上限 0）**。

**為什麼要有這一條**：帳本層完全健康，而**健康的帳本回答不了
「該進帳本的有沒有進來」** —— 那是兩個不同的問題，
而先前沒有任何東西在問後者。

---

## 4. 報價段（2026-08-26 已處理）

| 項目 | 狀態 |
|---|---|
| 線上明細編輯器 | 08-16 就完成，**08-26 才接進 PM 案件頁**（在那之前使用者到不了） |
| 範本 `quotation_template.xlsx` | 08-18（owner 提供的實際範本） |
| 稅額不隨小計重算 | **已修** —— 修法前 `total = 新小計 ＋ 舊稅額`（實測 8,000 + 12,656） |
| 測試 | 原本**零**，現有 4 個並驗過鑑別力（改回修法前 → 2 failed） |
| `erp_quotation_items` | **0 筆 / 256 張** ⇒ 已納管為生命跡象第 11 條 |

⚠️ `total_price` **混了兩種語意**：147 張稅額 = 總價 **5.00%**（未稅）、
66 張 = **4.76%**（＝5/105，含稅）。明細小計依定義是未稅，
所以使用者一旦改用明細填報，那張就統一到未稅 —— 收斂而非破壞，
且**只在他真的填了明細時才發生**。

---

## 5. 待決（需 owner 判斷）

| # | 議題 | 說明 |
|---|---|---|
| **F1** | 那 9 個執行中無金流的專案 | 是還沒到請款時點，還是漏了？前 4 個合計 1,155 萬 |
| **F2** | 28 個已結案且合約金額為 0 的舊案 | 要不要回填金額？不填的話毛利與營收統計永遠少這一段 |
| **F3** | 請款/應付要不要直接掛 `project_code` | 現在走三跳。多一跳不是錯，但**每個查詢都要記得**，而漏掉會得到 0 不是報錯 |

---

## 6. 判準

1. **健康的下游回答不了上游的問題** —— 帳本 0 缺口，
   而 9 個案子的錢根本沒進到請款單。
2. **用猜的欄位值查詢會得到看起來很嚴重的假結論** ——
   `source_type` 猜錯讓我一度以為 71 筆沒進帳本。
3. **同一個數字拆開之後可能是兩件事** —— 37 個「無金流」拆成
   28 個舊資料（$0）與 9 個執行中（$1,155 萬），
   後者才需要行動，而合起來看只會得到一個 42% 的焦慮數字。
