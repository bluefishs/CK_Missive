# 整體架構覆盤與三個月規劃（2026-09-09，重啟後）

> `lifecycle: status=current reviewed=2026-09-09 owner=CK_Missive`
>
> 回答 owner 三件事：①架構與近期異動的整體樣貌；②文件／設定／待辦統整到哪裡；③接下來該做什麼、不該做什麼。
> 數字全部是 2026-09-09 12:1x 實測，寫下去就會過期——要現況查 `/api/health/detailed` 與 `OPEN_ITEMS_20260819.md` 檔頭。

## 一、現況快照

| 面向 | 實測 |
|---|---|
| 線上 | `v6.76 @ 768c8165` ＝ HEAD，未推送 0；公網 200（0.9s）；12:04 重啟後五步全綠（`reboot-pre-flight-20260908.md` §5） |
| 規模 | API 端點檔 36（端點 738 支）／前端頁 271 tsx、路由 141／services 417 檔／repositories 32／models 28／Alembic 143 |
| 測試 | 後端 320 檔（4,455 全過，基線 0）／前端 232 檔（2,960 全過，基線 0）／pre-push 快速版只擋新失敗 |
| 檢核 | 腳本 226 支；每日 17 步（容器 02:00）；每週 **132** 步（host 週日 02:30）；教訓 L1–L150；本 repo ADR 25 |
| 排程 | Missive 自身 6 支 Windows 排程 LastRunTime 全在今天；異地備份 30 份 dump（549 MB）＋附件 1,652＋報價單總表快照 |
| 主機 | WSL2 24 GB／swap 8 GB（**swap=0 未套用**）；ollama 4 模型齊；平臺 11 容器全 healthy |

## 二、近十天異動（08-30～09-09，402 commits）歸成五條主線

1. **金流資料正確性**（最多 commit）：09-04 十一輪（口徑四處錯、230 張總價系統性偏差、16 筆 PM 已承攬無承攬案）、09-07 稅務三欄與一票多案、09-08 匯入器讀到彙總區塊／未稅寫進含稅欄、09-09 財務儀表板兩個年度口徑差 1,693 萬。
   形狀一致：**每張表單獨看都對、鏈是斷的**（L140），或**分歧存在、結果相同、只是還沒發作**（L149）。
2. **宣告收斂**（owner 09-07「四分五裂」後）：收斂 A 角色＝權限唯一來源、收斂 B 選單表＝選單／路由／API 三邊來源、統計中心服務 `services/stats/`、篩選 schema `CaseListFilters`、民國年 `core/roc_date.py`、表頭篩選鍵 `buildServerFilters`。每一項的驗收都是「對應檢核可降級」。
3. **安全**：`/uploads` 1,642 附件公網可讀（L148）→ 認證路由＋附件層級規則；路由守衛 117 條照選單宣告擋；成案／刪除／費用核銷補閘門與範圍。
4. **檢核體系自身**：weekly 統整成 10 領域（只把「本週新紅」判紅）、表態閘門三腳、慢性紅名冊、文件生命週期檔頭、TESTING_MAP 自動產生。
5. **主機層**：A127 WSL 核心段錯誤（約 25 筆 docs commit 的證據鏈）、backend 假活取證法（py-spy sidecar）、09-08 21:26 硬當定案為 mock OOM（L150）。

## 三、覆盤判斷：四個結構性問題

### P1 檢核在長、收斂在追，方向還是相反的

| 週 | weekly 步數 | 非綠 |
|---|---|---|
| 08-30 | 94 | 15 |
| 09-06 | 111 | 38 |
| 09-09 | 132 | 44 |

三週 **+38 步、非綠 +29、刪除 0 支、判準降級 2 條**。L147 自己寫「檢核數量下降才是架構變緊的訊號」，而 09-09 一天又新增 5 支。
收斂 A／B／統計中心／篩選 schema／民國年五項是真的把第二份宣告刪掉了，但**每一次事故仍反射性地 +1 支檢核**。
慢性紅 10 支裡 9 支已登記，代表訊號有人收，但「登記」不是「處置」。

### P2 數字正確性的上限已經卡在資料，而資料的決定積壓在 owner

程式面已收斂到「同一份實作」，剩下的錯是**資料本身沒有憑證可判**：應付 #51 拆帳、型二兩案實收大於報價、主檔併寫兩筆、顏新元三筆、雙主檔 15 家、誤植成案怎麼下架。
這些不決，再多口徑鎖也只是把錯的資料算得一致。owner 待決總表已集中到 `OPEN_ITEMS_20260819.md` 檔頭「09-09 晚」，共 12 項。

### P3 主機層仍是未解的不確定性，而 session 本身也被證明是風險源

A127 十筆核心段錯誤未解；swap=0 實驗前提被削弱且未執行；mdsched 從未做過。
09-08 硬當的真因是本 repo session 自己跑的負向測試（L150）——host 層 pyguard 已封頂，本 repo 測試規範已補，但**這條規範抓不到的地方在對話裡**，護欄只能是 host 層。

### P4 常駐文件的負載與「搬移慣例」沒有被自己執行

`CLAUDE.md` 09-09 中午 30.5 KB 常駐、每個 session 載入；其中 08-30／08-31 兩段比已經歸檔的 09-01～03 更早卻沒搬，09-04 十一輪也留在檔頭。
本次依既有慣例搬入 `MILESTONES_ARCHIVE.md`，檔頭降到 15.4 KB。同型：TESTING_MAP「自動產生」卻抄了手抄的「114 步」（實際 132）、weekly runner 檔頭「12 步」——**自動產生的表如果抄了一個常數，它就不是自動的**。

## 四、三個月規劃（驗收＝刪檢核＋owner 待決清零）

### Wave 0（本週，owner 決策）

| 決定 | 建議 |
|---|---|
| A127＋A132 主機層 | 先查 08-11 三筆 Windows 更新；要停機的（swap=0＋memtest Extended）**合併排一次夜間停機**，不要分兩次 |
| A129 長期紅 9 支 | 逐支「撤」或「留＋到期日」；撤掉的從 runner 移除——這是第一批「刪檢核」 |
| A130 誤植成案 | 先做 D1 唯讀預覽 |
| A131 promote 範圍 | 直接補 `assert_case_scope`（超管不受限），除非「替別人成案」是現行作法 |
| 資料面 6 項 | 拆帳金額／型二性質／併寫拆不拆／#39 錯字／雙主檔方案 A／備份表 10-04 DROP |

### Wave 1（9 月下旬，工程：收斂第三輪）

順序依「刪掉哪支檢核」排：
1. `UserRole` 列舉改 DB 載入 → 刪 weekly 118
2. 前端權限標籤從 DB 產生 → 刪 weekly 119 ①
3. 經費指標剩 3 處（`billing_service`、`quotation_service` ×2）＋ `/contract-cases` 篩選收斂到 `buildServerFilters` → weekly 132 基線歸零
4. 端點手抄轉接層（`projects/crud.py` QueryParams 直傳 schema）
5. 「所有 API 走 React Query」先量存量再決定要不要守門（存量若上百，立了也是天天紅）

**規則：新增檢核凍結**——只有伴隨一項收斂（有一份宣告被刪）才可新增；純「發現」只記 OPEN_ITEMS。

### Wave 2（10 月，資料面）

A113 備份表 DROP（10-04 後）、雙主檔補鍵、名稱快照退場（約 90 處讀快照的匯出／AI 工具分批改讀主檔）、拆帳與型二依 owner 給的數字執行（工具已備）。

### Wave 3（10–11 月，使用者面）

RWD 依 `FLOW_REVIEW_20260909.md` §四 順序：條件式豁免（沒有 `mobileCard` 的表格必須計入）→ 768–991px 斷點統一（`EnhancedTable` 768 vs `ResponsiveTable` 992）→ 統計卡網格守門擴到 `<Statistic>`（328 處）；owner 給 3–5 個常用手機頁。卡片↔下拉同步三處；表頭篩選第二、三批。

### Wave 4（11–12 月，平臺）

真實負載壓測（locust，需隔離環境）、前端 RUM、Cloudflare colo（AaaP 提案）。三者都有代價，值不值得由 owner 判。

### 停手清單（不做）

- 不再每個事故反射性 +1 支檢核（見 Wave 1 規則）
- 不動 keep-warm／不切 groq（A127 實驗汙染；免費 tier TPM 不夠）
- 不做「已註銷」狀態（B 方案口徑擴散 92 處）
- 不代跑跨 repo 推送或批次寫入（權限不轉包）
- 不把 MagicMock 負向測試做成靜態檢核（抓不到對話裡的指令；護欄在 host 層）

### 指標（10 月底複驗）

| 指標 | 現在 | 目標 |
|---|---|---|
| weekly 非綠 | 44 | ≤ 15（回到 08-30 水位） |
| weekly 步數 | 132 | 不增（收斂刪的比新增多） |
| owner 待決 | 12 | ≤ 3 |
| `CLAUDE.md` 常駐 | 15.4 KB | < 15 KB（每次 /doctor 搬日記段） |

## 五、本次已辦（09-09 晚）

- 重啟後五步檢查全綠；每日 02:00 三紅燈為部署前舊映像所致（容器內複跑 0／10 GREEN、14 YELLOW）。
- L150 入冊；`.claude/rules/testing.md` 負向測試記憶體邊界；事件單納入版控（AaaP 交辦 B-MISSIVE-MOCK-OOM 三項）。
- `swap-zero-experiment` 與 `reboot-pre-flight` 兩份 runbook 補執行紀錄；A127 與 OOM 事件正式分家。
- TESTING_MAP 產生器改讀 runner 步數；兩條過期敘述訂正。
- `CLAUDE.md` 日記段搬入歸檔（30.5 → 15.4 KB）；`OPEN_ITEMS` 檔頭新增 owner 決策總表；新登記 A133（backend `GET /` 500，低）。

## 六、09-09 晚補記：規劃執行結果與修正後的建議

| 規劃項 | 結果 |
|---|---|
| Wave 0 owner 決策 | A127 升為今晚必做（第五波 28+ 筆、五容器四 repo）；A135（R1 三筆補日期）新增；A129／A130／A105／A113／A103 未變 |
| Wave 1 收斂 | ✅ 統計中心服務化：weekly 132 Core 存量 13 處歸零；篩選之家四處＋統計端點吃列表 schema（133：12→7）；search／keyword 合一；`CaseFilterBar` 四頁（135）。owner 四次回報全部修好＋守門（134 請款佔位一致性、翻頁 flow、vitest ×3、pytest ×6 檔） |
| 新教訓 | L151（檢核守一致守不住定義）、L152（懶初始化把秒級故障放大成全面 500） |
| 我自己的錯 | 三件（截斷 scheduler、執行中改部署腳本、自造分母），都已修並各補一道守門或程序 |

**修正後的建議順序**（比 §四 多一條、前移一條）：

1. **主機層先於一切**：程式面今天已證明擋不住段錯誤，只能不放大它（L152 已做）。今晚 mdsched Extended 或 08-12 更新回退，擇一；驗收＝dmesg 計數 4 小時不動。
2. **只該做一次的初始化全部搬到開門前**（L152 同族清查）：快取預熱、單例、mapper 已做；下一輪掃 `services/` 裡「第一次用到才 init」的東西（可用 weekly 判準：`_INITED`／`lazy`／`if not self._x:` 型樣），這是唯一能讓高頻重啟環境不惡化的程式面工作。
3. Wave 1 剩餘：`UserRole` DB 載入（刪 118）、權限標籤 DB 產生（刪 119①）、weekly 133 剩 7 組由 owner 判性質。
4. Wave 2／3／4 不變（資料面、RWD、平臺）。

**優化程序（每輪固定）**：owner 回報 → 先量分母（全庫、不是自己那份）→ 找定義而不是找程式 → 修在中心服務一處 → 守門伴隨收斂（新增即紅、存量入基線）→ 部署後**讀探針那一行**（不是看 exit）→ 視覺走查一張圖 → 記進 OPEN_ITEMS 檔頭與教訓冊。

