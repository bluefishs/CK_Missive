# 標案與報價單：整體複查、議題盤點與規劃

> 建立：2026-08-26
> 觸發：owner 三則指示 ——
> ① 「兩個標案資料來源到底如何對應與互補，請釐清架構與服務」
> ② 「45% 仍是不合理」
> ③ 「線上報價單檢視及編輯維護 `/pm/cases` 尚未看到對應 UI 或功能。
> 　　整體流程應：**新增報價 → 線上報價單編輯 → 輸出 PDF →
> 　　自動依報價單內容建構邀標案件紀錄與對應專案同仁資訊**。
> 　　目前仍無正常運作，請複查確認」
>
> **所有數字都是 2026-08-26 實測。標明「未查證」的地方就是我沒查。**

---

## 第一部分：標案雙來源

### 1.1 owner 說「45% 仍是不合理」—— 是對的，因為那個數字不該用來衡量機制

逐月拆開後，答案完全改變：

| 月份 | ezbid | 已配 | 配對率 | 同期 PCC |
|---|---:|---:|---:|---:|
| 2026-04 | 12,922 | 1 | **0%** | **30** |
| 2026-05 | 15,643 | 2,637 | **17%** | **3,706** |
| 2026-06 | 9,415 | 8,886 | **94%** | 22,638 |
| 2026-07 | 998 | 972 | **97%** | 24,308 |
| 2026-08 | 10,519 | 9,940 | **94%** | 15,319 |

**PCC 有資料的月份，配對率一律 94–97%。**
排除 04–05 後的真實效能是 **94.6%**（19,798 / 20,932）。

45% 的分母裡有 **28,565 筆**（04–05 月的 ezbid）是「PCC 那段根本沒抓到」造成的。

### 1.2 那個缺口的成因，寫在我們自己的程式碼註解裡

`scheduler.py`：

> 「PCC scraper 自 **2026-04-08 起 50 天 silent dormant**（scheduler 缺 cron），
> 05-27 才補回」

⇒ 04 月 PCC 只有 30 筆。**那段 ezbid 資料本來就沒有對象可配**，不是配對失敗。

反過來 07 月是 ezbid 幾乎沒有（998 筆）而 PCC 有 24,308 筆 ——
**兩個來源的斷層時間不同，這正是雙來源的價值**：任一邊斷了，另一邊還在。

### 1.3 這個缺口補不補得回來

| 路徑 | 可行性 | 依據 |
|---|---|---|
| PCC `today` 端點 | **不行** | 只給當日，沒有歷史查詢 |
| openfun API 依 `org_id`+`job_number` | **部分** | 需要案號，而 04–05 月的 ezbid 舊格式 **100% 沒有 job_number** |
| openfun `searchbytitle` 逐筆查 | **未查證** | 端點存在且回 JSON（實測 HTTP 200），但**沒有評估過 28,565 次外部請求的成本與禮貌性** |

**建議：不補。** 理由是那段資料的業務價值低（2026-04~05 的標案早已截止），
而代價是近 3 萬次外部請求。**若 owner 認為需要，我再評估分批方案。**

### 1.4 標案其餘議題現況

| 議題 | 狀態 |
|---|---|
| 配對演算法 | ✅ pg_trgm 已廢止（中文 similarity 恆 0），改兩階段精確鍵 |
| 決標欄位補料 | ✅ `detail_enrichment` 已接排程（每日 03:45）。在此之前**全 repo 零 import、從未執行過** |
| 決標金額 | ⚠️ **不會是 100%** —— 總決標金額**視機關是否公開**，那是資料性質不是缺陷 |
| 抓取穩定性 | ✅ 已加第九條生命跡象（平日整天沒有新寫入就報）。此前 08-16~17 曾停 48 小時無人知 |
| 前端定位 | ✅ 查無時改為明說「編號不完整」而非「查無此標案」 |
| `budget` 覆蓋 | PCC 4 筆 / ezbid 48,841 筆 ⇒ **配對後可讓 PCC 端取用 ezbid 的預算**（尚未做，見 §3 建議 A3） |

---

## 第二部分：報價單流程四步複查

owner 的期望流程與實際落差：

| # | 期望 | 現況 | 判定 |
|---|---|---|---|
| ① | 新增報價 | PM 案件詳情頁有「新增報價」按鈕（08-20 加），導到 `ERP_QUOTATION_CREATE` 並帶入 case_code/case_name/year | ✅ **可用** |
| ② | 線上報價單檢視/編輯 | `ERPQuotationFormPage` / `ERPQuotationDetailPage` 都存在且可用 —— **但 `/pm/cases` 詳情頁沒有任何入口通往它們** | ❌ **入口缺席** |
| ③ | 輸出 PDF | `POST /erp/quotations/export-document`（08-19 落地，LibreOffice 轉檔、實測 200／`%PDF-1.7`／1.45s） | ✅ **可用** |
| ④ | 自動建構邀標案件與專案同仁 | **從未實作** | ❌ **不存在** |

### 2.1 ② 的具體證據

```
PM 案件 253 個｜其中有線上報價單的 229 個
```

**229 個案件有線上報價單，而案件詳情頁完全沒有列出它們。**

那個「報價紀錄」分頁是 `AttachmentPanel` —— 顯示的是**上傳的檔案**
（PDF/掃描件），與「線上報價單記錄」是兩回事。
⇒ 使用者開了報價單之後，**回到案件頁就找不到它**。只有出去的路，沒有回來的路。

### 2.2 ④ 的具體證據

* `grep` 報價單建立流程中**沒有任何** `PMCase` / `pm_cases` 的寫入；
* 反向也沒有；
* 實測：**256 張報價單有 27 張的 `case_code` 在 `pm_cases` 找不到**
  （全部是 2026-07-31 建立、`category='01'` 委辦招標）⇒ **孤兒**。

**而「對應專案同仁資訊」在資料模型上還不存在**：

* `pm_cases` **沒有任何同仁欄位**（`created_by` 是「誰建的」，不是「承辦誰」）；
* `backend/app/api/endpoints/pm/staff.py` 是**空 router**
  （檔頭：「已遷移至統一人員表 v5.2.0，保留空 router 確保 include_router 不報錯」）；
* 專案同仁只存在於**成案之後**的 `contract_projects` ＋ `project_staff`。

⇒ ④ 不只是「沒寫程式」，是**邀標階段目前沒有同仁這個概念**。

---

## 第三部分：建議與優先序

### A. 可以直接做（不需決策，缺口明確）

| # | 事項 | 為什麼 |
|---|---|---|
| **A1** | **PM 案件詳情頁列出該案的線上報價單** | 229 個案件受影響。這是純入口問題，不動資料模型。列表帶：報價單號／金額／狀態／填報者，點擊進 `ERPQuotationDetailPage` |
| **A2** | **27 張孤兒報價單** | 有 `case_code` 卻沒有對應案件。先出報表給 owner 看是哪些，**不自動補建**（補建等於替 owner 決定那些案子存在） |
| **A3** | **配對後把 ezbid 的 budget 補到 PCC 端** | 17,610 組是「ezbid 有預算而 PCC 沒有」。純資料庫運算、零外部請求 |

### B. 需要 owner 決定（涉及資料模型或業務語意）

| # | 議題 | 選項 |
|---|---|---|
| **B1** | **④「自動建構邀標案件」的觸發時機** | (a) 報價單建立時，若 case_code 對應的案件不存在就自動建 (b) 維持現況：先建案件再開報價（現在的動線） (c) 兩者都允許，但自動建的標記來源 |
| **B2** | **④「專案同仁」要不要下放到邀標階段** | 現在同仁只在成案後（`project_staff`）。若要在報價階段就指定，需在 `pm_cases` 加欄位或新增關聯表 —— **這是資料模型變更，我不擅自做** |
| **B3** | 04–05 月標案缺口要不要補 | 見 §1.3。建議不補 |

### C. 觀察中，不阻斷

| # | 事項 |
|---|---|
| C1 | `detail_enrichment` 目前每日只跑 40 筆（節流 0.8s／筆）⇒ 補完近 7 天待補的 7,703 筆需要約 半年。**要不要提高額度取決於 openfun 的禮貌性上限，尚未評估** |
| C2 | 決標金額覆蓋率會停在某個數字（機關未公開者永遠取不到），**首次跑滿一輪後才知道那個數字是多少** |

---

## 第三部分之二：owner 2026-08-26 回覆與執行結果

> owner：「1 兩者皆可但已填資訊要自動帶入，不然表要輸入案件也要輸入等於重複作業
> 　　　　 2 填寫報價單就要對應同仁，不然後續如何管理與對應維護
> 　　　　 3 完善架構在處理舊案；以及相關需求之前皆有提出為何會遺失」

### ③ 先回答質問：需求沒有遺失，但比遺失更難處理

`docs/architecture/ONLINE_QUOTATION_DESIGN.md`（**2026-08-17 建**）完整記著
owner 的原始需求，並拆成**五個決策**。實測執行狀況：

| 決策 | 現況 |
|---|---|
| 決策 1：報價單提前到邀標階段就存在 | ✅ **已執行** —— 256 張裡 180 張未成案（08-17 當時是 **0**） |
| 決策 4：便捷填報（逐項工項） | ❌ `erp_quotation_items` **0 筆** |
| 案件頁看得到線上報價單 | ❌ 從未做（2026-08-26 已補） |
| 自動建案件＋同仁 | ❌ 從未做 |

⇒ **需求被記錄、被設計、被拆成五個決策，然後只執行了其中一部分 ——
而「哪些做了哪些沒做」沒有任何東西在追蹤。**

這與本 repo 反覆記的「訊號存在但沒有接收者」是同一族，
差別在於它發生在**需求層**：既有的檢核全都在看程式與資料，
**沒有一支在看「設計文件裡的決策有沒有被執行」**。

### ② 已修：案件頁看得到線上報價單

229 個案件受影響。分頁改為上下兩區：**線上報價單**（金額／狀態／承辦同仁／
填報者，點擊進詳情）＋**報價單檔案**（原本的附件面板）。commit `8819661a`。

### ⚠️ 我在執行 ② 的過程中差點造成第二份實作 —— 已回退

我先建了 migration 在 `erp_quotations` 與 `pm_cases` 各加 `pm_user_id`，
理由是「兩張表都沒有同仁欄位」。**那個前提是錯的**：

```
project_user_assignments   139 筆 / 121 個 case_code / 11 位使用者
有承辦同仁的報價單          119 張
```

承辦同仁的機制**早就存在**（08-21 的 `staff_name` 欄位就是讀它），
而 PM 案件詳情頁也**早就有「承辦同仁」分頁**。
⇒ 我加的欄位是**第二份**，migration 已 downgrade、ORM 欄位已移除、
檔案已刪除，資料庫驗證 `pm_user_id` 欄位數 = **0**。

**這正是這個 session 一路在記錄的那個錯誤，而我自己犯了。**
發現它的方式是查證「既有機制是什麼」而不是「有沒有這個欄位」——
**問「有沒有欄位」會得到「沒有」，問「這件事現在怎麼做」才會得到真相。**

### ① 與 ② 剩下的部分（下一步）

| # | 事項 | 說明 |
|---|---|---|
| **N1** | **報價單表單加「承辦同仁」選擇** | owner：「填寫報價單就要對應同仁」。現況 256 張只有 119 張有 ⇒ 137 張沒有。寫入既有的 `project_user_assignments`，**不加新欄位** |
| **N2** | **建立時雙向帶入** | owner：「已填資訊要自動帶入，不然等於重複作業」。從案件建報價單已帶 case_code/case_name/year，**還缺委託單位與承辦同仁**；反向（從報價單建案件）**完全沒有** |
| **N3** | **決策執行追蹤** | 針對 ③ 的根因：設計文件裡的決策需要有東西在追。建議在 `ONLINE_QUOTATION_DESIGN.md` 每個決策標上狀態，並納入 `doc_baseline_claim_audit` 那類的納管 |

---

## 第四部分：這一輪的判準

1. **總體百分比會藏住真相** —— 45% 拆成逐月之後，
   「機制有問題」變成「機制 94.6%，而歷史有一段沒有對象可配」。
   **owner 說不合理是對的，但不合理的是那個指標不是那個機制。**
2. **一個永遠不會成功的機制每天執行，比沒有這個機制更糟** —— 它讓人以為有在配。
3. **「有出去的路、沒有回來的路」是一種完整的缺陷** ——
   ② 的每一個零件都能用，合起來卻不成流程。
4. **`grep` 零結果要當成發現而不是查詢失敗** ——
   ④ 就是這樣確認的（報價單建立流程裡沒有任何 PM 案件寫入）。
