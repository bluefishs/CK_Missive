"""
名稱正規化工具函數

集中管理實體名稱清理與比對用的工具函數，
供 graph_helpers、graph_merge_strategy 等模組共用。

Version: 1.0.0
Created: 2026-04-08
"""

import re
import unicodedata


# 公司 / 機關名稱正規化用的後綴
_ORG_SUFFIXES = re.compile(r'(股份有限公司|有限公司|事務所|分公司|分局|工務段)$')


def normalize_for_match(name: str) -> str:
    """正規化名稱用於模糊比對（NFKC + 去空白 + 去常見後綴）"""
    s = unicodedata.normalize("NFKC", name)
    s = re.sub(r'\s+', '', s).strip()
    s = _ORG_SUFFIXES.sub('', s)
    return s


def clean_agency_name(raw: str) -> str:
    """清理機關名稱：去除統編前綴、換行符"""
    s = raw.strip().replace('\n', '').replace('\r', '')
    s = re.sub(r'^\d{8,10}\s*', '', s)
    return s.strip() if s.strip() else raw.strip()


# 代名詞/泛稱黑名單 — 這些不是有效的命名實體
# 供 entity_extraction 和 canonical_entity 跨模組共用
PRONOUN_ENTITY_BLACKLIST: frozenset[str] = frozenset({
    # 代名詞（貴/本/該 + 機關稱謂）
    '貴公司', '本公司', '該公司', '貴所', '本所', '該所',
    '貴局', '本局', '該局', '貴府', '本府', '該府',
    '貴會', '本會', '該會', '貴處', '本處', '該處',
    '貴署', '本署', '該署', '貴部', '本部', '該部',
    '貴院', '本院', '該院', '貴市', '本市', '該市',
    '貴機關', '本機關', '該機關', '貴單位', '本單位', '該單位',
    '台端', '臺端',
    # 公文套語/例行用語 — 非實質關鍵字
    '檢送', '函送', '檢附', '檢陳', '函復', '函覆', '函請',
    '敬請', '請查照', '查照', '請照辦', '照辦', '惠請', '鑒核',
    '敬陳', '敬會', '敬悉', '如說明', '如主旨', '復如說明',
    '奉核', '奉悉', '如擬', '准予備查', '准予核備',
    '核定', '備查', '鑒察', '鑒查', '轉陳', '轉請',
    '諒達', '敬請鑒核', '敬請查照', '敬請備查',
    '檢送本公司', '函送本公司', '檢附本公司',
    '承辦人', '主管', '機關', '單位',
    # 動作詞（不是實體名稱）
    '檢退', '檢還', '退件', '退回',
    # 佔位符 / 簡體 / 無意義亂碼
    '實體名', '服务器',
    '司练南大家八室', '布八室', '服加八室',
})
