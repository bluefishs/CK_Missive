---
title: 功能模組 5.知識地圖
type: topic
sources: [site_navigation_items, router/AppRouter.tsx, api/endpoints, api/routes.py, ui-sweep.json]
tags: [功能模組, 履歷, 整合, auto-compiled]
confidence: high
---

> 由 `scripts/dev/feature_dossier.py` 組出，**不要手改**。
> 這是使用者語言的履歷：一個功能模組用到哪些頁面、API、後端模組，以及**誰在看它**。

# 功能模組履歷：5.知識地圖

## 使用者看到的（12 個項目）
- 圖譜與 Wiki 中樞　`/ai/graphs`　→ frontend/src/pages/GraphHubPage.tsx
- RAG圖譜　`/ai/knowledge-graph`　→ frontend/src/pages/KnowledgeGraphPage.tsx
- 標案圖譜　`/tender/graph`　→ frontend/src/pages/TenderGraphPage.tsx
- 代碼圖譜　`/ai/code-graph`　→ frontend/src/pages/CodeGraphManagementPage.tsx
- ERP 財務圖譜　`/ai/erp-graph`　→ frontend/src/pages/ERPGraphPage.tsx
- 資料庫圖譜　`/ai/db-graph`　→ frontend/src/pages/DatabaseGraphPage.tsx
- Skills技能圖譜　`/ai/skills-map`　→ frontend/src/pages/SkillsCapabilityMapPage.tsx
- 技能演化樹　`/ai/skill-evolution`　→ frontend/src/pages/SkillEvolutionPage.tsx
- 知識文庫　`/admin/knowledge-base`　→ frontend/src/pages/KnowledgeBasePage.tsx
- 記憶中樞　`/ai/memory`　→ frontend/src/pages/MemoryDashboardPage.tsx
- LLM Wiki　`/ai/wiki`　→ frontend/src/pages/WikiPage.tsx

- ⚠️ 1 個項目對應不到前端頁面檔（動態路由或非 SPA 路由）：代碼圖譜(/ai/code-wiki)

## 它打哪些 API —— 目前無法可信回答
- 靜態追 import 得不出可信答案：頁面經 `from '../hooks'` 這類
  barrel re-export 取用，追下去會把全 app 的端點都吸進來。
  實測鑑別力：深度 3 時公文頁得到 42 個端點而**含 document 字樣者 0%**，
  深度 5 爆到 168 個。依 SELF_AUDIT_EVOLUTION_STANDARD §3，
  驗不出鑑別力的維度不得交付 —— 所以這裡不給清單，而不是給一份錯的。
- **正確的解法是 runtime 而非靜態推論**：讓瀏覽器走查在開啟頁面時
  記錄實際發出的請求（引擎已有 read_network_requests 能力），
  那是事實不是推論。與第 6 階價值層改用 Prometheus 真實流量同一個道理。

## 誰在看它
- 瀏覽器走查涵蓋：**11/11** 條路由
