# Environments
.env
.env.*
!.env.example
!frontend/.env.production

# Docker Secrets (Phase 1)
secrets/

# Databases
*.db
*.sqlite3

# Python
__pycache__/
*.py[cod]
*$py.class

# Node
node_modules/
dist/
# 部署腳本的暫時建置目錄（vite build --outDir dist_next 後覆蓋進 dist、rm -rf）；與 dist 同類。備份 /XD 亦排除（2026-09-05 CK_Website 回報 678 筆 NOT_FOUND）
dist_next/
.npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE / OS
.vscode/
.idea/
.DS_Store

# Office 暫存/鎖定檔
~$*.pptx
~$*.docx
~$*.xlsx

# 簡報素材（不納管）
docs/presentations/*.pptx

# Vite cache
.vite/
frontend/.vite/

# Logs
*.log
backend/logs/
logs/

# Backups (binary dumps + redis snapshots not for git)
backups/database/*.sql
backups/database/*.sql.gz
backups/database/*.dump
# 2026-08-29：備份產物一律不進版控 —— 狀態檔每次備份都變（A40①），
# CSV 是修資料前的表快照。原規則只列 .sql/.dump，於是它們每天出現在
# `git status` 裡，而「一直有雜訊」與「有東西沒提交」長得一樣。
backups/database/_backup-status.json
backups/database/*.csv
backups/*.dump
backups/redis/
!backups/database/.gitkeep

# 刪除/清理前的業務資料備份 CSV。
# 2026-08-10 覆盤發現：這個目錄從來沒有規則涵蓋，於是 08-05／08-07 兩份
# 業務資料 CSV 已經進了版控，而 08-10 刪除重複承攬案件時匯出的兩份正等著被 commit。
# 這類檔案的用途是「刪錯了可以還原」，只需要留在本機；進版控等於把業務資料
# （案名／得標金額／委託單位）永久寫進 git 歷史，之後想拿掉就得改寫歷史。
backups/manual/

# Docker volume mount 副本 (wiki/ is SSOT, backend/wiki/ is docker mount mirror)
backend/wiki/

# Root SOUL.md duplicate (wiki/SOUL.md is SSOT)
/SOUL.md

# Temporary files
*.tmp
*.temp
nul

# Test files (root level only, exclude tests/ directory)
test*.csv
/test*.py
/debug*.py
!/backend/tests/
!/frontend/src/test/

# E2E / Playwright test results
frontend/test-results/
**/test-results/

# Test coverage
backend/.coverage
.coverage
htmlcov/
frontend/coverage/
frontend/coverage-summary.json

# Pytest cache
.pytest_cache/
backend/.pytest_cache/

# Screenshots
*.png

# Build artifacts
.claude/settings.local.json

# Import data files (CSV)
data/imports/csv_data/
data/imports/*.csv

# Sensitive credentials
backend/*.json
!backend/package.json
backend/credentials.json
backend/GoogleCalendarAPIKEY.json
backend/ck-missive-OAuth*.json

# Claude Code temporary directories
tmpclaude-*/
**/tmpclaude-*/
**/tmpclaude-*-cwd

# Backend uploads and backups (data files)
backend/backups/
backend/uploads/
backups/attachments/

# Root level test files
/minimal_test.py
/test_login.html
/package-lock.json

# Temporary Windows paths
C\357\200\272*

# Data files
data/ngrok_recovery_codes.txt

# Editor swap files
*.swp
*.swo
*~

# Screenshot files (Chinese named)
*.jpg
入口頁面.jpg

# Frontend auto-generated types (regenerated via npm run api:generate)
frontend/src/types/generated/api.d.ts

# Legacy config files (superseded by .env and docs/)
/monitoring_config.json
/port-config.json

# Temporary nav items file
**/nav_items.json

# Temporary data extraction files
backend/sheet*.txt
openapi_temp*.json

# 註：原 #BUG/ 已於 5/18 重命名為 docs/templates/import-templates/（業務 xlsx 範本，入版控）

# Deployment packages
*.tar.gz

# Archive directories (deprecated files)
/archive/
configs/archive/
scripts/archive/
docs/archive/sql/
docs/archive/data/
docs/archive/assets/

# Backend scattered scripts (moved to backend/scripts/)
backend/scripts/archive/

# Ruff cache
.ruff_cache/
backend/.ruff_cache/

# Pip install artifacts
backend/=*

# Claude planning files (5/18 移至 docs/archived/2026-05/claude_plant/)
# claude_plant/  # 已 archive

# Knowledge map (generated, import to Heptabase)
docs/knowledge-map/
docs/knowledge-map.zip

# GitNexus generated artifacts
knowledge_graph.json
.claude/code_graph_mtime.json

# Temporary data files
dispatch*.json
!frontend/src/**/*.json

# 註：#BUG/ 已 archive（5/18），規則保留註解避免誤判

# Runtime lock files
backend/.startup.lock
.claude/scheduled_tasks.lock

# Hermes Phase 0 — soul fidelity eval outputs (產出檔，每次執行覆寫)
scripts/checks/.soul-eval-*.json

# 2026-05-21 L43 incident: 雙 volume dump (本地 + NAS 異地保存，不入 git)
backup/incident_*/*.dump
backup/incident_*/*.sql.gz
.checks-state/

scripts/checks/sso_race_repro.png
scripts/checks/sso_entry_smoke.png
backups/code_graph_prune/

# Codex CLI 本地配置（AGENTS.md 為 CLAUDE.md 鏡像；避免第二份會漂移的配置入版控，2026-07-21）
# CLAUDE.md 為唯一 SSOT；如需 Codex 平價再改為手動同步入版控
AGENTS.md
.codex/
.agents/

# 自我檢核截圖（每次執行覆寫，不入版控）
docs/health/ui_flow_smoke_shots/
docs/health/ui_page_sweep_shots/

# 負向測試的暫存備份（2026-08-16：曾誤 commit 一個）
*.py.bak

# owner 2026-08-29：「地上物調查/ 未納版控 非本案作業」
docs/templates/地上物調查/

# pytest 稽核軌跡隔離（conftest 設的 CK_LOGS_DIR，2026-08-30）
backend/tests/.artifacts/

# 容器重啟偵測的 runtime 狀態（每次執行改寫，不是設定）
scripts/checks/.container_restart_state.json
