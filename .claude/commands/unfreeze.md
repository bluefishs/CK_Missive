# Unfreeze — 解除編輯範圍鎖定

解除 `/freeze` 設定的編輯範圍限制。

## 使用方式

```
/unfreeze     # 解除 freeze 鎖定
```

## 工作流程

1. **檢查** `.claude/freeze-scope.json` 是否存在
2. **若存在** — 讀取並顯示當前鎖定範圍，然後刪除檔案
3. **若不存在** — 回報「目前沒有啟用的 freeze 鎖定」
4. **確認訊息** — 「Freeze 已解除，所有目錄均可編輯。」
