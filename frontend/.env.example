# .env.example - CK_Missive 前端環境變數範本
# 複製此檔案為 .env.local 並填入實際值
#
# 環境檔案載入優先順序：
# - .env.local (本機覆蓋，不納入版控)
# - .env.development / .env.production (依環境載入)
# - .env (預設值)
#
# @version 1.0.0
# @date 2026-01-11

# ============================================================================
# API 配置
# ============================================================================
# 後端 API 基礎 URL（不含 /api 後綴）
# 留空則使用預設值 http://localhost:8001
#
# 系統會依據存取 IP 自動調整：
# - localhost 存取 → 使用 localhost:8001
# - 內網 IP 存取 → 使用相同主機 IP:8001
VITE_API_BASE_URL=http://localhost:8001

# 前端開發伺服器埠號
VITE_PORT=3000

# ============================================================================
# 認證配置
# ============================================================================
# 停用認證機制 (僅供開發使用，生產環境務必設為 false)
# true  = 停用認證，所有請求繞過登入
# false = 啟用認證，使用 Google OAuth 或傳統登入
#
# 智慧環境偵測：
# - 內網 IP (192.168.x.x, 10.x.x.x, 172.16-31.x.x) → 自動繞過認證
# - localhost → 使用 Google OAuth 登入
# - 公網存取 → 使用完整認證流程
VITE_AUTH_DISABLED=false

# Google OAuth 2.0 Client ID
# 用於 Google 登入功能，需在 Google Cloud Console 取得
# 文件：https://developers.google.com/identity/gsi/web/guides/get-google-api-clientid
VITE_GOOGLE_CLIENT_ID=

# ============================================================================
# 環境標識
# ============================================================================
# 當前環境類型
# development = 開發環境
# production  = 生產環境
NODE_ENV=development

# ============================================================================
# 開發環境專用配置
# ============================================================================
# 啟用 React 嚴格模式 (僅開發環境生效)
# VITE_STRICT_MODE=true

# 啟用詳細日誌輸出
# VITE_DEBUG_MODE=true

# ============================================================================
# 範例：不同環境配置
# ============================================================================
# 本機開發（使用 Docker 後端）：
#   VITE_API_BASE_URL=http://localhost:8001
#   VITE_AUTH_DISABLED=false
#
# 區域網路測試：
#   VITE_API_BASE_URL=http://192.168.50.38:8001
#   VITE_AUTH_DISABLED=false
#
# ngrok 隧道測試：
#   VITE_API_BASE_URL=https://abc123.ngrok-free.app
#   VITE_AUTH_DISABLED=false
#
# 生產環境：
#   VITE_API_BASE_URL=https://api.yourdomain.com
#   VITE_AUTH_DISABLED=false
#   VITE_GOOGLE_CLIENT_ID=your-production-client-id
