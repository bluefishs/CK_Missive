# =============================================================================
# Multichannel Bot Stack — LINE / Telegram / Discord Webhooks
# =============================================================================
# ADR-0014 (2026-04-14): OpenClaw 已由 Hermes Agent 取代，原 openclaw service
#   於 2026-04-23（NemoClaw Sprint 2）正式從本檔案移除。
#   歷史見：docs/archive/nemoclaw-archival-checklist.md
#
# Hermes 部署：CK_AaaP/runbooks/hermes-stack/docker-compose.yml
# 主通道：LINE / Telegram webhook → CK_Missive backend(:8001) 直連
# =============================================================================

name: ck_multichannel

# v5.9.2 後本檔無實際 service，保留 networks 宣告供其他 compose 引用相容
networks:
  ck_infra:
    external: true
    name: ${COMPOSE_PROJECT_NAME}_default
